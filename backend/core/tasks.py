# This file can be used for background tasks (e.g. cleaning expired share links).
# For demonstration, we leave it empty or we can implement a simple cleanup routine.

def cleanup_expired_share_links():
    # Pseudocode:
    # from django.utils import timezone
    # from .models import ShareLink
    # ShareLink.objects.filter(expires_at__lt=timezone.now()).delete()
    pass
